{
  "name": "Json server",
  "description": "Serves the inflatable JSON layout files to be polled for updates by the example project",
  "method": "GET",
  "responseCode": 200,
  "generates": "indexPage"
}
